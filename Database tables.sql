CREATE TABLE cars(
  brand VARCHAR(255),
  model VARCHAR(255),
  year INT,
  primary key (brand)
  );

  INSERT INTO cars (brand,model,year)
  VALUES ('Ford','Mustang',1964);

  Select * from cars;
 
